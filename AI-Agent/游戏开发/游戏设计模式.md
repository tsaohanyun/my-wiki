---
title: 游戏设计模式
aliases:
  - Game Design Patterns
  - 游戏编程模式
  - Game Programming Patterns
tags:
  - design-patterns
  - architecture
  - game-development
  - programming
type: wiki
status: active
created: 2026-06-28
updated: 2026-06-28
source: AI-Agent知识库
difficulty: intermediate
project: 游戏开发
---

# 游戏设计模式

游戏开发中常用的设计模式，解决状态管理、对象复用、事件通信、输入处理等核心问题。

---

## 一、状态机模式（State Machine）

用于管理角色状态、AI 行为、游戏流程等。

### 1.1 通用状态机实现

```csharp
using System;
using System.Collections.Generic;

// 状态接口
public interface IState
{
    void OnEnter();
    void OnUpdate(float deltaTime);
    void OnExit();
    bool CanTransitionTo(string stateName);
}

// 状态机构建器
public class StateMachine
{
    private Dictionary<string, IState> states = new Dictionary<string, IState>();
    private IState currentState;
    private string currentStateName;

    public string CurrentStateName => currentStateName;

    public void AddState(string name, IState state)
    {
        states[name] = state;
    }

    public void ChangeState(string newStateName)
    {
        if (!states.ContainsKey(newStateName))
        {
            UnityEngine.Debug.LogError($"状态 {newStateName} 不存在");
            return;
        }

        if (currentState != null)
        {
            if (!currentState.CanTransitionTo(newStateName)) return;
            currentState.OnExit();
        }

        currentState = states[newStateName];
        currentStateName = newStateName;
        currentState.OnEnter();
    }

    public void Update(float deltaTime)
    {
        currentState?.OnUpdate(deltaTime);
    }
}
```

### 1.2 角色状态示例

```csharp
using UnityEngine;

public class IdleState : IState
{
    private PlayerController player;

    public IdleState(PlayerController player)
    {
        this.player = player;
    }

    public void OnEnter()
    {
        player.Animator.Play("Idle");
        player.Velocity = Vector3.zero;
    }

    public void OnUpdate(float deltaTime)
    {
        if (player.InputMagnitude > 0.1f)
        {
            player.StateMachine.ChangeState("Run");
        }
        if (player.JumpPressed)
        {
            player.StateMachine.ChangeState("Jump");
        }
    }

    public void OnExit() { }

    public bool CanTransitionTo(string stateName)
    {
        return true; // Idle 可切换到任何状态
    }
}

public class AttackState : IState
{
    private PlayerController player;
    private float attackTimer;

    public AttackState(PlayerController player)
    {
        this.player = player;
    }

    public void OnEnter()
    {
        player.Animator.Play("Attack");
        player.CanMove = false;
        attackTimer = 0.5f; // 攻击持续时间
    }

    public void OnUpdate(float deltaTime)
    {
        attackTimer -= deltaTime;
        if (attackTimer <= 0f)
        {
            player.StateMachine.ChangeState("Idle");
        }
    }

    public void OnExit()
    {
        player.CanMove = true;
    }

    public bool CanTransitionTo(string stateName)
    {
        // 攻击中只能被受击/死亡打断
        return stateName == "Hit" || stateName == "Die";
    }
}
```

### 1.3 有限状态机的分层（HFSM）

```
┌─────────────┐
│   Locomotion │  ← 上层状态
├─────────────┤
│  ┌───┐ ┌───┐ ┌───┐  │
│  │Idle│ │Run │ │Jump│  │  ← 下层状态
│  └───┘ └───┘ └───┘  │
├─────────────┤
│   Combat     │
├─────────────┤
│  ┌─────┐ ┌─────┐  │
│  │Attack│ │Block │  │
│  └─────┘ └─────┘  │
└─────────────┘
```

---

## 二、对象池模式（Object Pool）

避免频繁 `Instantiate`/`Destroy`，减少 GC 压力。

### 2.1 通用对象池

```csharp
using UnityEngine;
using System.Collections.Generic;

public class ObjectPool<T> where T : Component
{
    private Queue<T> pool = new Queue<T>();
    private T prefab;
    private Transform parent;

    public ObjectPool(T prefab, int initialSize, Transform parent = null)
    {
        this.prefab = prefab;
        this.parent = parent;

        // 预热
        for (int i = 0; i < initialSize; i++)
        {
            T obj = Object.Instantiate(prefab, parent);
            obj.gameObject.SetActive(false);
            pool.Enqueue(obj);
        }
    }

    public T Get()
    {
        T obj;
        if (pool.Count > 0)
        {
            obj = pool.Dequeue();
        }
        else
        {
            obj = Object.Instantiate(prefab, parent);
        }
        obj.gameObject.SetActive(true);
        return obj;
    }

    public void Return(T obj)
    {
        obj.gameObject.SetActive(false);
        pool.Enqueue(obj);
    }

    public void ReturnDelayed(T obj, float delay)
    {
        // 使用协程延迟回收
        ReturnAfterDelay(obj, delay);
    }

    private async void ReturnAfterDelay(T obj, float delay)
    {
        await System.Threading.Tasks.Task.Delay((int)(delay * 1000));
        if (obj != null && obj.gameObject != null)
        {
            Return(obj);
        }
    }
}
```

### 2.2 子弹池使用示例

```csharp
using UnityEngine;

public class BulletSpawner : MonoBehaviour
{
    [SerializeField] private Bullet bulletPrefab;
    [SerializeField] private int poolSize = 50;

    private ObjectPool<Bullet> bulletPool;

    void Awake()
    {
        bulletPool = new ObjectPool<Bullet>(bulletPrefab, poolSize, transform);
    }

    public void Fire(Vector3 position, Vector3 direction)
    {
        Bullet bullet = bulletPool.Get();
        bullet.transform.position = position;
        bullet.Initialize(direction, OnBulletHit);
    }

    private void OnBulletHit(Bullet bullet)
    {
        bulletPool.Return(bullet);
    }
}

public class Bullet : MonoBehaviour
{
    [SerializeField] private float speed = 20f;
    [SerializeField] private float lifetime = 3f;

    private Vector3 direction;
    private System.Action<Bullet> onHit;
    private float timer;

    public void Initialize(Vector3 dir, System.Action<Bullet> hitCallback)
    {
        direction = dir;
        onHit = hitCallback;
        timer = lifetime;
    }

    void Update()
    {
        transform.position += direction * speed * Time.deltaTime;
        timer -= Time.deltaTime;

        if (timer <= 0f)
        {
            onHit?.Invoke(this);
        }
    }

    void OnTriggerEnter(Collider other)
    {
        // 造成伤害
        onHit?.Invoke(this);
    }
}
```

### 2.3 Unity 2021+ 内置 Pooling

```csharp
using UnityEngine;
using UnityEngine.Pool;

public class ModernBulletSpawner : MonoBehaviour
{
    [SerializeField] private Bullet bulletPrefab;

    private ObjectPool<Bullet> pool;

    void Awake()
    {
        pool = new ObjectPool<Bullet>(
            createFunc: () => Instantiate(bulletPrefab),
            actionOnGet: (b) => b.gameObject.SetActive(true),
            actionOnRelease: (b) => b.gameObject.SetActive(false),
            maxSize: 100
        );
    }
}
```

---

## 三、观察者模式（Observer / Event System）

解耦模块间通信。

### 3.1 类型安全的事件总线

```csharp
using System;
using System.Collections.Generic;

public static class EventBus
{
    private static Dictionary<Type, List<Delegate>> subscribers = new();

    public static void Subscribe<T>(Action<T> handler) where T : struct
    {
        var type = typeof(T);
        if (!subscribers.ContainsKey(type))
            subscribers[type] = new List<Delegate>();

        subscribers[type].Add(handler);
    }

    public static void Unsubscribe<T>(Action<T> handler) where T : struct
    {
        var type = typeof(T);
        if (subscribers.ContainsKey(type))
            subscribers[type].Remove(handler);
    }

    public static void Publish<T>(T eventData) where T : struct
    {
        var type = typeof(T);
        if (!subscribers.ContainsKey(type)) return;

        // 拷贝列表防止迭代中修改
        var handlers = new List<Delegate>(subscribers[type]);
        foreach (var handler in handlers)
        {
            ((Action<T>)handler).Invoke(eventData);
        }
    }

    public static void Clear()
    {
        subscribers.Clear();
    }
}

// 事件定义
public struct PlayerDeathEvent
{
    public int PlayerId;
    public Vector3 DeathPosition;
}

public struct EnemyKilledEvent
{
    public string EnemyName;
    public int Score;
}

public struct ItemPickedUpEvent
{
    public string ItemName;
    public int Quantity;
}
```

### 3.2 使用示例

```csharp
// 发布者
public class Enemy : MonoBehaviour
{
    public void Die()
    {
        EventBus.Publish(new EnemyKilledEvent
        {
            EnemyName = gameObject.name,
            Score = 100
        });
        Destroy(gameObject);
    }
}

// 订阅者
public class ScoreManager : MonoBehaviour
{
    private int score;

    void OnEnable()
    {
        EventBus.Subscribe<EnemyKilledEvent>(OnEnemyKilled);
    }

    void OnDisable()
    {
        EventBus.Unsubscribe<EnemyKilledEvent>(OnEnemyKilled);
    }

    private void OnEnemyKilled(EnemyKilledEvent e)
    {
        score += e.Score;
        Debug.Log($"击杀 {e.EnemyName}，得分: {score}");
    }
}
```

---

## 四、命令模式（Command）

将输入操作封装为对象，支持撤销/重做、录像回放、输入缓冲。

### 4.1 命令基础结构

```csharp
using UnityEngine;

// 命令接口
public interface ICommand
{
    void Execute();
    void Undo();
}

// 具体命令：移动
public class MoveCommand : ICommand
{
    private Transform target;
    private Vector3 direction;
    private float distance;
    private Vector3 previousPosition;

    public MoveCommand(Transform target, Vector3 direction, float distance)
    {
        this.target = target;
        this.direction = direction;
        this.distance = distance;
    }

    public void Execute()
    {
        previousPosition = target.position;
        target.position += direction * distance;
    }

    public void Undo()
    {
        target.position = previousPosition;
    }
}

// 具体命令：旋转
public class RotateCommand : ICommand
{
    private Transform target;
    private Vector3 axis;
    private float angle;
    private Quaternion previousRotation;

    public RotateCommand(Transform target, Vector3 axis, float angle)
    {
        this.target = target;
        this.axis = axis;
        this.angle = angle;
    }

    public void Execute()
    {
        previousRotation = target.rotation;
        target.Rotate(axis, angle);
    }

    public void Undo()
    {
        target.rotation = previousRotation;
    }
}
```

### 4.2 命令管理器（支持撤销/重做）

```csharp
using System.Collections.Generic;

public class CommandManager
{
    private Stack<ICommand> undoStack = new Stack<ICommand>();
    private Stack<ICommand> redoStack = new Stack<ICommand>();

    public void ExecuteCommand(ICommand command)
    {
        command.Execute();
        undoStack.Push(command);
        redoStack.Clear(); // 新操作清空重做栈
    }

    public void Undo()
    {
        if (undoStack.Count == 0) return;

        ICommand command = undoStack.Pop();
        command.Undo();
        redoStack.Push(command);
    }

    public void Redo()
    {
        if (redoStack.Count == 0) return;

        ICommand command = redoStack.Pop();
        command.Execute();
        undoStack.Push(command);
    }
}
```

### 4.3 输入缓冲（格斗游戏）

```csharp
using System.Collections.Generic;
using UnityEngine;

public class InputBuffer
{
    private struct BufferedInput
    {
        public ICommand Command;
        public float Timestamp;
    }

    private Queue<BufferedInput> buffer = new Queue<BufferedInput>();
    private float bufferWindow = 0.15f; // 150ms 缓冲窗口

    public void BufferInput(ICommand command)
    {
        buffer.Enqueue(new BufferedInput
        {
            Command = command,
            Timestamp = Time.time
        });
    }

    public ICommand ConsumeBufferedInput()
    {
        while (buffer.Count > 0)
        {
            var input = buffer.Dequeue();
            if (Time.time - input.Timestamp <= bufferWindow)
            {
                return input.Command;
            }
            // 过期的输入直接丢弃
        }
        return null;
    }
}
```

---

## 五、其他常用模式速查

| 模式 | 用途 | 适用场景 |
|------|------|----------|
| **单例 (Singleton)** | 全局唯一管理器 | GameManager、AudioManager |
| **策略 (Strategy)** | 可替换的算法 | AI行为、伤害计算 |
| **组件 (Component)** | 组合优于继承 | ECS架构、Unity组件 |
| **工厂 (Factory)** | 对象创建解耦 | 敌人生成、技能创建 |
| **中介者 (Mediator)** | 多对象协调 | UI系统、聊天系统 |
| **访问者 (Visitor)** | 不修改结构扩展操作 | 存档系统、序列化 |

---

## 六、最佳实践

| 实践 | 说明 |
|------|------|
| **状态机用于有限状态场景** | 状态少（<10）用 FSM，复杂 AI 用行为树 |
| **对象池预热 + 动态扩容** | 预分配常用量，溢出时自动创建 |
| **事件总线注意生命周期** | `OnDisable` 必须取消订阅，防止内存泄漏 |
| **命令模式配合序列化** | 可实现网络同步、录像回放 |
| **优先组合优于继承** | 用组件/策略模式代替深层继承 |
| **设计模式不要过度使用** | 简单问题简单解决 |

---

## 七、相关页面

- [[Unity开发入门]] - Unity 中实现这些模式
- [[Unreal Engine基础]] - UE 中的组件化与 Blueprint 模式
- [[游戏网络同步]] - 命令模式在帧同步中的应用
- [[游戏性能优化]] - 对象池与内存优化
