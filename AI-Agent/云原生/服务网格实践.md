---
title: 服务网格实践
aliases:
  - Service Mesh
  - Istio实践
  - 服务网格
tags:
  - 云原生
  - 服务网格
  - Istio
  - 流量管理
  - 可观测性
type: 文档
status: 已完成
created: 2024-02-01
updated: 2024-03-20
source: 内部实践
difficulty: 高级
project: AI-Agent
---

# 服务网格实践

## 概述

服务网格（Service Mesh）是一种基础设施层，用于处理服务间通信。Istio 是目前最流行的服务网格实现，提供流量管理、安全、可观测性等功能。

## Istio 安装配置

### 使用 Istioctl 安装

```yaml
# istio-operator.yaml
apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
metadata:
  name: istio-control-plane
  namespace: istio-system
spec:
  profile: default
  meshConfig:
    accessLogFile: /dev/stdout
    accessLogEncoding: JSON
    enableTracing: true
    defaultConfig:
      tracing:
        sampling: 100.0
      holdApplicationUntilProxyStarts: true
  components:
    pilot:
      k8s:
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        hpaSpec:
          minReplicas: 2
          maxReplicas: 5
    ingressGateways:
      - name: istio-ingressgateway
        enabled: true
        k8s:
          hpaSpec:
            minReplicas: 2
            maxReplicas: 10
          service:
            type: LoadBalancer
            ports:
              - port: 80
                targetPort: 8080
                name: http2
              - port: 443
                targetPort: 8443
                name: https
  values:
    global:
      proxy:
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 256Mi
```

```bash
# 安装
istioctl install -f istio-operator.yaml --verify

# 命名空间注入 sidecar
kubectl label namespace default istio-injection=enabled

# 验证安装
istioctl analyze
istioctl proxy-status
```

## 流量管理

### VirtualService - 流量路由

```yaml
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: myapp-vs
  namespace: default
spec:
  hosts:
    - myapp.example.com
  gateways:
    - myapp-gateway
  http:
    # 金丝雀发布：按权重分流
    - match:
        - headers:
            x-canary:
              exact: "true"
      route:
        - destination:
            host: myapp
            subset: canary
            port:
              number: 8080
    # 基于路径路由
    - match:
        - uri:
            prefix: /api/v2
      route:
        - destination:
            host: myapp
            subset: v2
            port:
              number: 8080
    # 默认路由
    - route:
        - destination:
            host: myapp
            subset: stable
            port:
              number: 8080
          weight: 90
        - destination:
            host: myapp
            subset: canary
            port:
              number: 8080
          weight: 10
      retries:
        attempts: 3
        perTryTimeout: 2s
        retryOn: gateway-error,connect-failure,refused-stream
      timeout: 10s
      fault:
        delay:
          percentage:
            value: 0.1
          fixedDelay: 5s
```

### DestinationRule - 负载均衡与熔断

```yaml
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: myapp-dr
  namespace: default
spec:
  host: myapp
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 100
        connectTimeout: 30ms
      http:
        h2UpgradePolicy: DEFAULT
        http1MaxPendingRequests: 100
        http2MaxRequests: 1000
        maxRequestsPerConnection: 10
        maxRetries: 3
    outlierDetection:
      consecutive5xxErrors: 5
      interval: 10s
      baseEjectionTime: 30s
      maxEjectionPercent: 50
    loadBalancer:
      simple: LEAST_REQUEST
  subsets:
    - name: stable
      labels:
        version: stable
    - name: canary
      labels:
        version: canary
    - name: v2
      labels:
        version: v2
      trafficPolicy:
        connectionPool:
          http:
            http2MaxRequests: 500
```

### Gateway 配置

```yaml
apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: myapp-gateway
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 443
        name: https
        protocol: HTTPS
      tls:
        mode: SIMPLE
        credentialName: myapp-tls-credential
      hosts:
        - myapp.example.com
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - myapp.example.com
      tls:
        httpsRedirect: true
```

## 安全策略

### mTLS 配置

```yaml
# 全局 mTLS
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: istio-system
spec:
  mtls:
    mode: STRICT
---
# 命名空间级别
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: production
spec:
  mtls:
    mode: STRICT
---
# 工作负载级别
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: myapp-strict
  namespace: production
spec:
  selector:
    matchLabels:
      app: myapp
  mtls:
    mode: STRICT
  portLevelMtls:
    8080:
      mode: PERMISSIVE
```

### AuthorizationPolicy

```yaml
# 允许前端访问后端 API
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: backend-api-policy
  namespace: production
spec:
  selector:
    matchLabels:
      app: backend-api
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/production/sa/frontend
            namespaces:
              - production
      to:
        - operation:
            methods: ["GET", "POST"]
            paths: ["/api/*"]
      when:
        - key: request.headers[x-request-id]
          notValues: [""]
---
# 拒绝所有其他流量
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: deny-all
  namespace: production
spec:
  {}
---
# JWT 认证
apiVersion: security.istio.io/v1beta1
kind: RequestAuthentication
metadata:
  name: jwt-auth
  namespace: production
spec:
  selector:
    matchLabels:
      app: backend-api
  jwtRules:
    - issuer: "https://auth.example.com/"
      jwksUri: "https://auth.example.com/.well-known/jwks.json"
      audiences:
        - "api.example.com"
      forwardOriginalToken: true
```

## 可观测性

### Kiali - 服务拓扑

```yaml
apiVersion: kiali.io/v1alpha1
kind: Kiali
metadata:
  name: kiali
  namespace: istio-system
spec:
  auth:
    strategy: token
  deployment:
    accessible_namespaces:
      - "**"
  external_services:
    prometheus:
      url: http://prometheus:9090
    grafana:
      url: http://grafana:3000
    tracing:
      url: http://jaeger-query:16686
```

### Telemetry 配置

```yaml
apiVersion: telemetry.istio.io/v1alpha1
kind: Telemetry
metadata:
  name: mesh-logging
  namespace: istio-system
spec:
  accessLogging:
    - providers:
        - name: envoy
  metrics:
    - providers:
        - name: prometheus
  tracing:
    - providers:
        - name: jaeger
      randomSamplingPercentage: 10
---
# 工作负载级别
apiVersion: telemetry.istio.io/v1alpha1
kind: Telemetry
metadata:
  name: myapp-tracing
  namespace: production
spec:
  selector:
    matchLabels:
      app: myapp
  tracing:
    - providers:
        - name: jaeger
      randomSamplingPercentage: 100
```

### ServiceMonitor

```yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: istio-mesh-monitor
  namespace: monitoring
spec:
  selector:
    matchExpressions:
      - key: istio
        operator: In
        values: [pilot]
  endpoints:
    - port: http-monitoring
      interval: 15s
```

## 最佳实践

1. **渐进式接入**：先在测试环境验证，逐步启用 mTLS 和策略
2. **命名规范**：统一 VirtualService/DestinationRule/Gateway 命名
3. **资源调优**：合理配置 sidecar 代理的资源 limits
4. **排除不需要注入的 Pod**：使用 `sidecar.istio.io/inject: "false"` 注解
5. **避免过多 VirtualService**：合并同一服务的路由规则
6. **监控告警**：对接 Prometheus + Grafana 监控 mesh 指标
7. **定期升级**：跟踪 Istio 版本更新，及时修复安全漏洞
8. **故障排查**：善用 `istioctl analyze` 和 `istioctl proxy-config`
9. **多集群方案**：规划好多集群 service mesh 的网络拓扑
10. **性能测试**：上线前进行负载测试，评估 sidecar 的性能开销

## 相关页面

- [[Helm Charts开发]] - 使用 Helm 安装 Istio
- [[Operator开发指南]] - Istio Operator 实现
- [[云原生网络]] - 网络基础与 Service Mesh 互补
- [[云原生存储]] - 持久化 Istio 组件数据
